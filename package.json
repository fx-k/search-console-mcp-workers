{
  "name": "search-console-mcp-workers",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Read-only Google Search Console and Bing Webmaster Tools Remote MCP on Cloudflare Workers",
  "scripts": {
    "check": "node --check src/index.js && node --check src/oauth.js && node --check src/oauth-state.js && node --check src/crypto.js && node --check src/mcp.js && node --check src/google.js && node --check src/bing.js && node --check src/pagespeed.js && node --check src/intelligence.js && node --check src/technical.js && node --check src/tools.js && node --check src/validation.js",
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
